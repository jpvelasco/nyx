# Opengrep defaults skip *_test.go; Codacy cloud scans test files.
# We re-enable test files here to match Codacy cloud, but the permission/path
# traversal rules fire on every os.WriteFile/os.ReadFile in test fixtures.
# Codacy cloud doesn't flag these (test files are excluded server-side),
# so exclude them locally too for parity.
# !*_test.go  — commented out to match cloud behavior

# npm shim and build scripts — not application code, use ESLint/PMD for those
npm/
scripts/